name: styled
description: A declarative UI tool for Flutter to simplify the creation of widgets, passing arguments in positional and without orders.
version: 0.1.0
homepage: https://github.com/rob333/styled

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
