name: styled
description: A declarative UI tool for flutter to simplify the creation of widgets, pass arguments in positional without order.
version: 0.0.10
homepage: https://github.com/rob333/styled

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
