name: styled
description: A declarative UI tool to simplify the creation of flutter widgets. Pass arguments in positional and without order.
version: 0.0.9
homepage: https://github.com/rob333/styled

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
